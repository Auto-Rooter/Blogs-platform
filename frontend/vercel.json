{
    "version": 2,
    "rootDirectory": "frontend",
    "installCommand": "npm install --legacy-peer-deps",
    "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then exit 0; fi; if git diff --quiet \"$VERCEL_GIT_COMMIT_SHA^\" \"$VERCEL_GIT_COMMIT_SHA\" -- frontend/; then exit 0; fi; exit 1",
    "buildCommand": "npm run build",
    "outputDirectory": "build"
}
  