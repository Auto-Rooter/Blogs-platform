{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "version": 2,
    "installCommand": "npm install --legacy-peer-deps",
    "buildCommand":   "npm run build",
    "outputDirectory":"build",
    "ignoreCommand":  "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then exit 0; fi; if git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" \"$VERCEL_GIT_COMMIT_SHA\" -- frontend/; then exit 0; fi; exit 1"
  }